<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.kindergarten.modules.equipment.dao.EventDao">
    <select id="getAll" resultType="com.example.kindergarten.modules.equipment.entity.Event">
        SELECT *
        FROM `AIevent`
        limit
        #{currentPage},#{pageSize}
    </select>


</mapper>